// Deprecated credential form retained only for reference
// Deprecated placeholder: the project now uses the dedicated LoginScreen.
const CredentialsForm = () => null

export default CredentialsForm
